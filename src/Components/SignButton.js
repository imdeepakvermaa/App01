import React from 'react'

function SignButton({text}) {
  return (
    <div>
        <button className='SignButton'>
            {text}
        </button>
    </div>
  )
}

export default SignButton;
